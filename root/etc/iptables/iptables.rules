*nat
:PREROUTING ACCEPT [0:0]
:INPUT ACCEPT [0:0]
:POSTROUTING ACCEPT [0:0]
:OUTPUT ACCEPT [0:0]
-A POSTROUTING -s CONFIG_OPENVPN_SUBNET -o CONFIG_LAN_INTERFACE -j MASQUERADE
-A POSTROUTING -s CONFIG_WIREGUARD_SUBNET -o CONFIG_LAN_INTERFACE -j MASQUERADE
COMMIT

*filter
:INPUT DROP [0:0]
:FORWARD DROP [0:0]
:OUTPUT ACCEPT [0:0]

-A INPUT -i lo -j ACCEPT
-A INPUT -i tun+ -j ACCEPT
-A INPUT -i wg+ -j ACCEPT
-A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT
-A INPUT -m state --state INVALID -j DROP

# Accept ICMP type 8 (ping)
-A INPUT -p icmp -m icmp --icmp-type 8 -m limit --limit 1/second -j ACCEPT

# Allow from WAN (OpenVPN and WireGuard)
-A INPUT -i CONFIG_LAN_INTERFACE -p udp --dport openvpn -m state --state NEW -j ACCEPT
-A INPUT -i CONFIG_LAN_INTERFACE -p udp --dport 51820 -m state --state NEW -j ACCEPT

# Allow from LAN and VPN
-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport ssh -m state --state NEW -j ACCEPT

# OpenVPN
-A FORWARD -i tun+ -o CONFIG_LAN_INTERFACE -j ACCEPT
-A FORWARD -i CONFIG_LAN_INTERFACE -o tun+ -j ACCEPT

# WireGuard
-A FORWARD -i wg+ -o CONFIG_LAN_INTERFACE -j ACCEPT
-A FORWARD -i CONFIG_LAN_INTERFACE -o wg+ -j ACCEPT

#
# Containers
#

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport http -m state --state NEW -j ACCEPT -m comment --comment "Homer (webui)"

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 50100 -m state --state NEW -j ACCEPT -m comment --comment "Pihole (webui)"
-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport domain -m state --state NEW -j ACCEPT -m comment --comment "Pihole (dns)"
-A INPUT -p udp -s CONFIG_TRUSTED_NETWORKS --dport domain -m state --state NEW -j ACCEPT -m comment --comment "Pihole (dns)"
-A INPUT -i eth0 -p udp -s 0.0.0.0 --sport bootpc -d 255.255.255.255 --dport bootps -j ACCEPT -m comment --comment "Pihole (dhcp)"

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 50101 -m state --state NEW -j ACCEPT -m comment --comment "sshwifty (webui)"

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 50102 -m state --state NEW -j ACCEPT -m comment --comment "Uptime-Kuma (webui)"

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 50103 -m state --state NEW -j ACCEPT -m comment --comment "OpenBudgeteer (webui)"

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 50104 -m state --state NEW -j ACCEPT -m comment --comment "Code server (webui)"

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 50105 -m state --state NEW -j ACCEPT -m comment --comment "Portainer-ce (webui)"
#-A INPUT -p tcp -s CONFIG_LAN_NETWORK --dport 8000 -m state --state NEW -j ACCEPT -m comment --comment "Portainer-ce (agent)"

-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 50106 -m state --state NEW -j ACCEPT -m comment --comment "Deluge (webui)"
-A INPUT -p tcp -s CONFIG_TRUSTED_NETWORKS --dport 6881 -m state --state NEW -j ACCEPT -m comment --comment "Deluge (inbound torrent)"
-A INPUT -p udp -s CONFIG_TRUSTED_NETWORKS --dport 6881 -m state --state NEW -j ACCEPT -m comment --comment "Deluge (inbound torrent)"

COMMIT
